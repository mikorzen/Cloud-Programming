//import type { Config } from "prettier";

export default {
	printWidth: 100,
	useTabs: true,
	tabWidth: 4,
	bracketSameLine: true,
	plugins: ["prettier-plugin-tailwindcss"],
}; //satisfies Config; can't do TypeScript sadge
